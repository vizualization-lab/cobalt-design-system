---
'@cobalt/components': patch
'@cobalt/tokens': patch
'@cobalt/icons': patch
'@cobalt/react': patch
'@cobalt/vue': patch
'@cobalt/angular': patch
---

Added a minimal `README.md` to each published package so the Verdaccio package page has descriptive content. The READMEs intentionally stay terse (tagline, one paragraph on what it is and how it relates to other Cobalt packages, an install snippet, and a pointer to the Cobalt docs site) so they don't become a second source of truth for content owned by the docs.
