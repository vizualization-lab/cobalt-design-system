---
'@cobalt/cli': minor
---

Add read-only project diagnostics and component lookup commands. The CLI now supports `co inspect`, `co doctor`, and `co components` with JSON output for agent and CI workflows.
