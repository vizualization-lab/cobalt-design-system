---
'@cobalt/cli': patch
---

Expanded `co agent component` with docs-derived, framework-aware usage guidance. The CLI now bundles component guidance generated from docs Usage tabs, selects Web Component, React, Vue, or Angular examples from project inspection or `--framework`, and keeps raw API metadata sourced from the custom elements manifest.
