---
'@cobalt/cli': minor
'@cobalt/docs': patch
---

Replaced the `create-cobalt` starter generator with the `@cobalt/cli` package and `co new` command. The CLI now supports user-level registry config in `~/.cobalt.config.json`, plus config commands for saving registry defaults before scaffolding starter projects.
