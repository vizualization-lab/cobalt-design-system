---
'@cobalt/components': patch
'@cobalt/docs': patch
'@cobalt/tokens': patch
---

Added semantic border-width intent tokens and updated bordered components to use the new `--co-border-width-*` contract, including the `action` outline role. Also fixed the token browser so the Border category label is displayed consistently.
