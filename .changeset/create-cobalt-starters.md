---
'create-cobalt': patch
'@cobalt/docs': patch
---

Added the `npm create cobalt` starter generator with vanilla TypeScript, React, Vue, and Angular templates, optional SCSS helper setup, app shell starter composition, registry configuration, local tarball dependency mode, generated project README and gitignore files, optimized framework subpath imports, and CLI flags for non-interactive scaffolding.
