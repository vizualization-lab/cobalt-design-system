---
'@cobalt/docs': patch
---

Added a Cobalt favicon to the docs site. The SVG variant adapts to the OS color scheme (dark slate on light browsers, light surface on dark browsers) so the icon stays neutral against any theme; an `.ico` is shipped as a multi-size legacy fallback.
