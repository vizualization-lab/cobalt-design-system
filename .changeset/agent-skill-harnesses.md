---
'@cobalt/cli': patch
---

Added multi-harness Cobalt skill installation to `co new`. Generated projects can now install the Cobalt agent skill for Codex, Claude Code, both, or neither via the new `--agent-skill` option, with `both` as the default for interactive and `--yes` flows.
