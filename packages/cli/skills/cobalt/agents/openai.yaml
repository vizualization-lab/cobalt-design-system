interface:
  display_name: 'Cobalt'
  short_description: 'Use the Cobalt CLI as the source of truth for components, tokens, utilities, and diagnostics.'
  default_prompt: 'Use $cobalt. Run `co agent context` first to learn this project, then query component, token, and utility metadata before editing.'
