{
  "name": "@cobalt/docs",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Cobalt Design System documentation",
  "scripts": {
    "prepare:local-deps": "pnpm --filter @cobalt/icons build && pnpm --filter @cobalt/vue build",
    "predev": "pnpm run prepare:local-deps && node scripts/generate-component-changelogs.js",
    "prebuild": "pnpm run prepare:local-deps && node scripts/generate-component-changelogs.js",
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "audit:contrast": "pnpm run build && node scripts/audit-contrast.js"
  },
  "devDependencies": {
    "@cobalt/components": "workspace:*",
    "@cobalt/icons": "workspace:*",
    "@cobalt/tokens": "workspace:*",
    "@cobalt/vue": "workspace:*",
    "markdown-it-task-lists": "^2.1.1",
    "mermaid": "^11.14.0",
    "shiki": "^2.5.0",
    "vite": "^5.4.21",
    "vitepress": "^1.5.0",
    "vitepress-plugin-mermaid": "^2.0.17",
    "vue": "^3.5.0"
  },
  "license": "MIT"
}
