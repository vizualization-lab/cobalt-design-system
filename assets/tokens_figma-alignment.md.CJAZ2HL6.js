import{_ as t,o,c as a,h as i}from"./chunks/framework.Bt9YOM9y.js";const m=JSON.parse('{"title":"Figma Token Alignment","description":"","frontmatter":{},"headers":[],"relativePath":"tokens/figma-alignment.md","filePath":"tokens/figma-alignment.md"}'),r={name:"tokens/figma-alignment.md"};function d(n,e,s,l,c,h){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="figma-token-alignment" tabindex="-1">Figma Token Alignment <a class="header-anchor" href="#figma-token-alignment" aria-label="Permalink to &quot;Figma Token Alignment&quot;">​</a></h1><p>This page audits the Figma variables currently present in the design system against the token model defined in this repository.</p><p>The goal is not to preserve every exported variable. The goal is to separate:</p><ul><li>values that already fit the repository model</li><li>missing semantic roles that are worth adding</li><li>experimental Figma-only layers that should be retired instead of adopted</li></ul><h2 id="executive-summary" tabindex="-1">Executive Summary <a class="header-anchor" href="#executive-summary" aria-label="Permalink to &quot;Executive Summary&quot;">​</a></h2><p>The Figma exports are not four parallel source-of-truths. They are four different stages of token maturity:</p><table tabindex="0"><thead><tr><th>Export</th><th>What it appears to be</th><th>Recommended disposition</th></tr></thead><tbody><tr><td><code>brand.json</code></td><td>Raw palette, typography, and spacing primitives</td><td>Keep only where it matches repository primitives</td></tr><tr><td><code>alias.json</code></td><td>Friendly aliases over brand primitives</td><td>Do not keep as a long-term production layer</td></tr><tr><td><code>semantic.json</code></td><td>A very small global semantic layer</td><td>Replace with repository semantic tokens</td></tr><tr><td><code>mapped.json</code></td><td>Component and state recipes built from lower layers</td><td>Treat as migration input, not as source-of-truth tokens</td></tr></tbody></table><p>Two things are true at the same time:</p><ol><li>Most of the raw color palette already aligns with the repository.</li><li>The Figma library currently exposes too many overlapping layers, which is what creates confusion during handoff.</li></ol><h2 id="what-already-aligns" tabindex="-1">What Already Aligns <a class="header-anchor" href="#what-already-aligns" aria-label="Permalink to &quot;What Already Aligns&quot;">​</a></h2><p>The strongest alignment is in raw color values:</p><ul><li><code>143</code> of the <code>146</code> exported Figma brand colors already exist as exact hex values in <code>packages/tokens/tokens/primitives.color.json</code>.</li><li>The exported Figma brand ramps map cleanly to the repository palette names: <code>blue</code>, <code>navy</code>, <code>teal</code>, <code>lime</code>, <code>purple</code>, <code>indigo</code>, <code>brick</code>, <code>rust</code>, <code>gold</code>, <code>red</code>, <code>green</code>, <code>orange</code>, <code>yellow</code>, and <code>neutral</code>.</li><li>The repository already has the correct architectural split for long-term governance: primitives, shared semantics, theme semantics, and selective component tokens.</li></ul><p>This means the main problem is not palette incompatibility. The main problem is that Figma currently contains overlapping abstraction layers.</p><h2 id="what-the-exports-mean" tabindex="-1">What The Exports Mean <a class="header-anchor" href="#what-the-exports-mean" aria-label="Permalink to &quot;What The Exports Mean&quot;">​</a></h2><h3 id="brand-json" tabindex="-1"><code>brand.json</code> <a class="header-anchor" href="#brand-json" aria-label="Permalink to &quot;`brand.json`&quot;">​</a></h3><p>This file is closest to a primitive token set.</p><p>It contains:</p><ul><li>the core color ramps</li><li>the Inter typography scale</li><li>the spacing scale</li></ul><p>It mostly matches the repository primitives, but it also carries a few values that the repository intentionally does not expose today:</p><ul><li>neutral steps <code>200</code>, <code>700</code>, and <code>1200</code></li><li>scale steps <code>75</code>, <code>250</code>, <code>700</code>, and <code>1100</code></li><li>a <code>26px</code> heading level that does not exist in the repository type scale</li></ul><h3 id="alias-json" tabindex="-1"><code>alias.json</code> <a class="header-anchor" href="#alias-json" aria-label="Permalink to &quot;`alias.json`&quot;">​</a></h3><p>This file looks like a translation layer over <code>brand.json</code>.</p><p>It renames:</p><ul><li><code>Primary-Colors/Blue/*</code> into <code>Primary/*</code></li><li>status ramps into <code>Status/*</code></li><li>typography primitives into <code>Text/Font-*</code></li></ul><p>This is useful during experimentation, but it creates duplication if designers can choose both the brand tokens and the alias tokens.</p><h3 id="semantic-json" tabindex="-1"><code>semantic.json</code> <a class="header-anchor" href="#semantic-json" aria-label="Permalink to &quot;`semantic.json`&quot;">​</a></h3><p>This file is a small semantic layer for:</p><ul><li>background roles</li><li>text roles</li></ul><p>It is directionally correct, but it is too small for the repository’s semantic model. It does not cover the full set of surface, border, interactive, and feedback roles already defined in <code>packages/tokens/tokens/semantic.theme.default.light.json</code> and <code>packages/tokens/tokens/semantic.theme.default.dark.json</code>.</p><h3 id="mapped-json" tabindex="-1"><code>mapped.json</code> <a class="header-anchor" href="#mapped-json" aria-label="Permalink to &quot;`mapped.json`&quot;">​</a></h3><p>This file is not a foundation layer. It is a usage-recipe layer.</p><p>Examples:</p><ul><li><code>Text/Button-Primary/default</code></li><li><code>Surface/Checkbox/Checked/hover</code></li><li><code>Border-Color/Radio/Off/error</code></li></ul><p>Those names encode component, variant, state, and context all at once. That is useful as an audit of what designers needed, but it should not become the token taxonomy in the repository.</p><p>Most of this layer should either:</p><ul><li>map back to existing semantic tokens</li><li>become component implementation logic</li><li>become a very small set of explicit component tokens only when the rule is truly reusable and stable</li></ul><h2 id="implemented-high-confidence-additions" tabindex="-1">Implemented High-Confidence Additions <a class="header-anchor" href="#implemented-high-confidence-additions" aria-label="Permalink to &quot;Implemented High-Confidence Additions&quot;">​</a></h2><p>These were the strongest candidates because they represented real semantic gaps, not just Figma duplication. They are now implemented in the repository token files.</p><table tabindex="0"><thead><tr><th>Candidate</th><th>Why it looks real</th><th>Evidence in Figma</th></tr></thead><tbody><tr><td><code>co.color.surface.page</code></td><td>Designers are using a page background that is distinct from <code>surface.default</code>, <code>surface.raised</code>, and <code>surface.sunken</code></td><td><code>semantic.Background.muted</code>, <code>mapped.Surface.page</code></td></tr><tr><td><code>co.color.surface.disabled</code></td><td>Disabled neutral surfaces appear repeatedly across controls and menus</td><td><code>mapped.Surface.disabled</code>, button/select/menu disabled mappings</td></tr><tr><td><code>co.color.text.placeholder</code></td><td>Placeholder text is treated as a first-class role, not just “secondary text”</td><td><code>mapped.Text/Default/placeholder</code></td></tr><tr><td><code>co.color.text.tertiary</code></td><td>Designers are using an intermediate low-emphasis text tone that does not match current <code>text.secondary</code> or <code>text.disabled</code></td><td><code>semantic.Text/secondary</code>, <code>mapped.Text/Default/caption</code></td></tr><tr><td><code>co.color.border.active</code></td><td>Active border color is reused across controls</td><td><code>mapped.Border-Color.active</code> and repeated control state mappings</td></tr><tr><td><code>co.color.border.danger</code></td><td>Error border state is reused across checkbox, radio, toggle, and fields</td><td><code>mapped.Border-Color.error</code></td></tr><tr><td><code>co.color.border.success</code></td><td>Success border state is reused across checkbox, radio, toggle, and fields</td><td><code>mapped.Border-Color.success</code></td></tr><tr><td><code>co.shape.border-width.lg</code></td><td>A <code>3px</code> border width exists as a separate Figma primitive; add only if the system wants to support it consistently</td><td><code>mapped.Border-Width.Lg</code></td></tr></tbody></table><h2 id="lower-confidence-additions" tabindex="-1">Lower-Confidence Additions <a class="header-anchor" href="#lower-confidence-additions" aria-label="Permalink to &quot;Lower-Confidence Additions&quot;">​</a></h2><p>These exist in Figma, but they look more like experimental history than clear repository gaps.</p><table tabindex="0"><thead><tr><th>Candidate</th><th>Why it is lower confidence</th></tr></thead><tbody><tr><td>Neutral steps <code>200</code>, <code>700</code>, <code>1200</code></td><td>These are exact palette gaps between current repository neutral steps, but they may have been removed intentionally to keep the palette smaller and clearer</td></tr><tr><td>Scale steps <code>75</code>, <code>250</code>, <code>700</code>, <code>1100</code></td><td>These introduce <code>3px</code>, <code>10px</code>, <code>28px</code>, and <code>44px</code> steps that may be useful, but they are not clearly part of the current repository scale strategy</td></tr><tr><td>Figma <code>Heading/H2 = 26px</code></td><td>This conflicts with the repository editorial scale more than it complements it</td></tr><tr><td>Figma <code>28px</code> line-height aliases</td><td>These show real usage, but the repository currently models line-height as ratios instead of fixed pixel values</td></tr><tr><td>Icon-specific semantic colors</td><td>These may be valuable for navigation and status-heavy UI, but many icons can still inherit text color successfully</td></tr></tbody></table><h2 id="tokens-that-should-not-be-added-as-is" tabindex="-1">Tokens That Should Not Be Added As-Is <a class="header-anchor" href="#tokens-that-should-not-be-added-as-is" aria-label="Permalink to &quot;Tokens That Should Not Be Added As-Is&quot;">​</a></h2><p>These layers are the main source of confusion and should be collapsed, not promoted.</p><h3 id="alias-json-1" tabindex="-1"><code>alias.json</code> <a class="header-anchor" href="#alias-json-1" aria-label="Permalink to &quot;`alias.json`&quot;">​</a></h3><p>Do not keep both raw brand primitives and alias primitives visible to designers in the production library.</p><p>Pick one primitive layer.</p><h3 id="most-of-mapped-json" tabindex="-1">Most of <code>mapped.json</code> <a class="header-anchor" href="#most-of-mapped-json" aria-label="Permalink to &quot;Most of `mapped.json`&quot;">​</a></h3><p>Do not add tokens like:</p><ul><li><code>Text/Button-Secondary/focus</code></li><li><code>Surface/Checkbox/Checked/success</code></li><li><code>Border-Color/Radio/Off/error</code></li></ul><p>Those are component state recipes. They belong in:</p><ul><li>component variant logic</li><li>component documentation</li><li>a very small component token layer when a public contract is required</li></ul><p>They do not belong in the global design token taxonomy.</p><h2 id="recommended-figma-integration-strategy" tabindex="-1">Recommended Figma Integration Strategy <a class="header-anchor" href="#recommended-figma-integration-strategy" aria-label="Permalink to &quot;Recommended Figma Integration Strategy&quot;">​</a></h2><p>The repository should remain the only source of truth.</p><p>The Figma library should become a projection of the repository, not a second token authoring system.</p><h3 id="_1-sync-from-the-repository-build-output" tabindex="-1">1. Sync from the repository build output <a class="header-anchor" href="#_1-sync-from-the-repository-build-output" aria-label="Permalink to &quot;1. Sync from the repository build output&quot;">​</a></h3><p>The token build already generates a Token Studio-compatible merged artifact at <code>packages/tokens/dist/tokens-merged.json</code>.</p><p>That file should become the only sync payload used to populate Figma variables.</p><h3 id="_2-publish-fewer-collections-in-figma" tabindex="-1">2. Publish fewer collections in Figma <a class="header-anchor" href="#_2-publish-fewer-collections-in-figma" aria-label="Permalink to &quot;2. Publish fewer collections in Figma&quot;">​</a></h3><p>The current Figma setup exposes too many overlapping choices. Prefer this production structure:</p><ol><li><code>Cobalt Primitives</code></li><li><code>Cobalt Semantics</code></li><li><code>Cobalt Components</code></li></ol><p>Recommended behavior:</p><ul><li><code>Cobalt Primitives</code> contains only raw scales and palettes</li><li><code>Cobalt Semantics</code> contains the tokens designers should use day to day</li><li><code>Cobalt Components</code> stays intentionally small and only covers real public component contracts</li></ul><h3 id="_3-use-figma-modes-for-theme-differences" tabindex="-1">3. Use Figma modes for theme differences <a class="header-anchor" href="#_3-use-figma-modes-for-theme-differences" aria-label="Permalink to &quot;3. Use Figma modes for theme differences&quot;">​</a></h3><p>Do not model light and dark as separate ad hoc collections when the difference is semantic.</p><p>Instead:</p><ul><li>keep shared semantic roles in the semantic collection</li><li>expose <code>Light</code> and <code>Dark</code> modes in Figma</li><li>let theme-varying semantic values change by mode</li></ul><p>That mirrors the repository’s <code>semantic.shared</code> plus <code>semantic.theme.&lt;theme&gt;.&lt;mode&gt;</code> structure without making designers think about file boundaries.</p><h3 id="_4-hide-or-lock-primitives-for-most-designers" tabindex="-1">4. Hide or lock primitives for most designers <a class="header-anchor" href="#_4-hide-or-lock-primitives-for-most-designers" aria-label="Permalink to &quot;4. Hide or lock primitives for most designers&quot;">​</a></h3><p>Designers building product UI should mostly consume semantic tokens.</p><p>Primitives should remain available for:</p><ul><li>design system maintainers</li><li>new token definition work</li><li>debugging and audits</li></ul><p>This reduces accidental “value shopping” in the Figma variable picker.</p><h3 id="_5-retire-the-experimental-figma-layers-from-production" tabindex="-1">5. Retire the experimental Figma layers from production <a class="header-anchor" href="#_5-retire-the-experimental-figma-layers-from-production" aria-label="Permalink to &quot;5. Retire the experimental Figma layers from production&quot;">​</a></h3><p>Once repository-backed collections exist, the current exploratory sets should be treated like migration artifacts:</p><ul><li>retire <code>Alias</code></li><li>retire the current mini <code>Semantic</code> set</li><li>retire <code>Mapped</code> as a token collection</li></ul><p>If these files are still useful, keep them only as audit references in the playground or migration workspace.</p><h3 id="_6-convert-component-recipes-into-component-documentation" tabindex="-1">6. Convert component recipes into component documentation <a class="header-anchor" href="#_6-convert-component-recipes-into-component-documentation" aria-label="Permalink to &quot;6. Convert component recipes into component documentation&quot;">​</a></h3><p>The best parts of <code>mapped.json</code> are signals about what designers need:</p><ul><li>supported states</li><li>repeated disabled treatments</li><li>repeated status treatments</li><li>recurring page or surface roles</li></ul><p>Use that file to:</p><ul><li>decide which semantic gaps to fill in the repository</li><li>verify component state coverage in Figma</li><li>update handoff documentation</li></ul><p>Do not keep it as a production variable collection.</p><h3 id="_7-add-one-governance-rule-for-every-new-figma-token" tabindex="-1">7. Add one governance rule for every new Figma token <a class="header-anchor" href="#_7-add-one-governance-rule-for-every-new-figma-token" aria-label="Permalink to &quot;7. Add one governance rule for every new Figma token&quot;">​</a></h3><p>Every proposed token should answer this question first:</p><p>Which repository layer does this belong to?</p><p>The allowed answers are:</p><ul><li>primitive</li><li>shared semantic</li><li>theme semantic</li><li>component</li></ul><p>If the answer is “it is a button hover recipe” or “it is a checkbox success border,” it is probably not a new global token.</p><h2 id="suggested-migration-order" tabindex="-1">Suggested Migration Order <a class="header-anchor" href="#suggested-migration-order" aria-label="Permalink to &quot;Suggested Migration Order&quot;">​</a></h2><ol><li>Import repository primitives and repository semantic tokens into a clean Figma library branch.</li><li>Add the high-confidence semantic gaps from this page before asking designers to migrate.</li><li>Remap current component library instances from <code>Alias</code>, <code>Semantic</code>, and <code>Mapped</code> to the new repository-backed collections.</li><li>Publish light and dark semantic modes from the repository token sets.</li><li>Remove the exploratory collections from the production library after the component library no longer depends on them.</li></ol><h2 id="decision-framing" tabindex="-1">Decision Framing <a class="header-anchor" href="#decision-framing" aria-label="Permalink to &quot;Decision Framing&quot;">​</a></h2><p>If the team wants the least confusing designer-developer handoff, the principle should be:</p><p>Figma variables are a distribution channel for repository tokens, not an independent taxonomy.</p><p>That keeps naming, implementation, and handoff anchored to one system instead of four overlapping experiments.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./structure.html">Token Structure</a></li><li><a href="./figma-handoff.html">Figma Handoff</a></li><li><a href="./">Token Reference</a></li></ul>',98)])])}const u=t(r,[["render",d]]);export{m as __pageData,u as default};
